"""ExpensasPagas app."""

# Django
from django.apps import AppConfig


class ExpensasPagasAppConfig(AppConfig):
    """ExpensasPagas app config."""

    name = 'apps.platforms.expensas_pagas'
    verbose_name = 'ExpensasPagas'
