Descargar el proyecto desde git 
    git clone https://anopa@bitbucket.org/anopa/admincufull.git

Instalar docker 
    https://docs.docker.com/engine/install/ubuntu/
    Instalar docker-compose
        sudo apt-get install docker-compose
    Iniciar
        sudo service docker start

Instalar Node y npm 
    https://medium.com/@DanielSayidi/install-and-setup-react-app-on-ubuntu-18-04-3-lts-fcd2c875885a

cd react/
    npm install
    sudo chown -R $mariano:$(id -gn $mariano) /home/mariano/.config (Esto lo solicitó react)
    npm audit fix --force
    sudo chown -R $USER:$(id -gn $USER) /home/mariano/.config (Me lo volvio a solicitar asi que lo ejecuté literal)

    cd ../


sudo docker-compose -f devServer.yml build postgres
sudo docker-compose -f devServer.yml build redis
# Aqui hay que comentar algunas lineas (la ultima vez fueron 11 archivos) que consultan a la base de datos sin que la misma esté migrada
sudo docker-compose -f devServer.yml build django
sudo docker-compose -f devServer.yml run --rm django python manage.py migrate
sudo docker-compose -f devServer.yml run --rm django python manage.py createsuperuser
sudo docker-compose -f devServer.yml build react
sudo docker-compose -f devServer.yml run --rm react npm rebuild node-sass
sudo docker-compose -f devServer.yml up



Que se puedan eliminar los asientos
Arreglar pdfs


Agregar utilizacion de disponibilidades en proveedores

Backend

    Carpeta 
        con arbol de dependencias
        que pueda guardar mas carpetas adentro
    
    Analitica
        Igual que en contabilidad