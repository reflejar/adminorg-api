# Default values for coreapi.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 3

image:
  repository: anopa/adminsmart_django
  pullPolicy: Always
  # Overrides the image tag whose default is the chart appVersion.
  tag: "latest"

imagePullSecrets:
  - name: docker-secret
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

podAnnotations: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

service:
  type: ClusterIP
  port: 8000

ingress:
  enabled: false
  className: ""
  annotations:
    kubernetes.io/ingress.class: nginx
    kubernetes.io/tls-acme: "true"
  hosts:
    - host: api.admin-smart.com
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls:
   - secretName: api.admin-smart-tls
     hosts:
       - api.admin-smart.com

resources: {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80

nodeSelector: {}

tolerations: []

affinity: {}

configMapVars:
  REDIS_URL: "redis://redis:6379/0"
  CELERY_FLOWER_USER: "f59a7aba6f436f1ed2e43769f7bdb861"
  CELERY_FLOWER_PASSWORD: "5f864a54f4e40fb8c363efb6aacbbee68615ea6a"
  POSTGRES_HOST: "kfdjnavweyuv87y54v8h238v.digitalocean.db.postgres.1298378123"
  POSTGRES_PORT: "54877"
  POSTGRES_DB: "adminsmart"
  POSTGRES_USER: "anopa"
  DJANGO_SETTINGS_MODULE: "config.settings.production"

secretsKeyVars:
  POSTGRES_PASSWORD: "SjlGTHhwSlhWdwo="
