apiVersion: v1
kind: Service
metadata:
  name: {{ include "coreapi.fullname" . }}
  labels:
    {{- include "coreapi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "coreapi.selectorLabels" . | nindent 4 }}
